In the array:

flintstones = %w(Fred Barney Wilma Betty BamBam Pebbles)

Find the index of the first name that starts with "Be"

Understand the problem:
  given an array of names, find the index of the first element in the array that 
  begins with the string "Be"
  
Input:
  an array of names
  
Output:
  the index of the first name that starts with "Be"
  
Logic:
  iterate through the array using .index whose first two chars == "Be"
